# 任务：命令注入检查工具

## 上下文
- 目标：基于codex exec对目标项目进行命令注入静态检查，识别显式与隐式脚本解释器调用。
- 约束：仅引用本地证据，输出固定JSON，支持后续扩展检查类型。

## 计划
- [x] 1. 设计命令注入检查提示词与固定JSON输出规范。
- [x] 2. 实现Python工具：按检查类型调用codex exec并落盘JSON。
- [x] 3. 更新架构与变更记录文档。

## 进度记录
- 已完成：1、2、3
- 进行中：
- 待开始：

# 计划：命令注入检查-预扫描提示增强

## 上下文

- 需求：提示词补充“外部命令调用应避免任何可能导致命令注入的方式”。
- 需求：在 `tools/command_injection_check.py` 增加基于 `rg` 的候选点预扫描，并注入提示词以辅助分析。
- 约束：保持输出结构不变，提示词仅增强引导。

## 计划

- [x] 更新 `prompts/command_injection_check.md`，新增安全提示与预扫描占位区。
- [x] 更新 `tools/command_injection_check.py`，新增 `rg` 预扫描并注入提示词变量。
- [x] 更新 `doc/changelog.md` 记录本次修改目的/范围/内容/影响。

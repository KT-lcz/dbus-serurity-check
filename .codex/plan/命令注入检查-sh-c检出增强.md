# 任务：命令注入检查 sh -c 检出增强

## 上下文
- 需求：提升对 `sh -c` 场景的检出率，覆盖数组/变量组合（如 `cmdArgs = []string{"sh", "-c"}`）。
- 约束：保持输出结构不变，仅优化提示词与必要的工具配置。

## 计划
- [x] 1. 补充提示词的检索指引与显式执行示例，强调数组/变量场景。
- [x] 2. 更新变更记录与计划状态。

## 进度记录
- 已完成：1、2
- 进行中：
- 待开始：
